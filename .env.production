# Supabase (public, client-side). Safe to commit + ship: the publishable/anon key
# is RLS-protected — it can only touch the signed-in user's own rows. Vite inlines
# these into the build (CI reads this file in production mode). NEVER add the
# sb_secret_... key here.
VITE_SUPABASE_URL=https://pbmskbotlkbgmohcsbea.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_namgZHLc9-O4eIOhp6U8Ig_LgR4lJNK
